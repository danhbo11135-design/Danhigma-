local DanhSigma = {
    Version = "6.0",
    Author = "Danh Sigma Team",
    ImageID = "98607865358715",
    
    -- Features
    AutoFarm = false,
    AutoRaid = false,
    AutoDungeon = false,
    AutoSeaBeast = false,
    AutoCollectFruits = false,
    AutoStoreFruits = false,
    ServerHop = false,
    AutoBuyHaki = false,
    
    -- Settings
    FarmSpeed = 0.2,
    FarmDistance = 20,
    FruitPriority = "Mythical",
    Webhook = "",
    
    -- Stats
    FruitsCollected = 0,
    BeliEarned = 0,
    LevelsGained = 0,
    StartTime = os.time()
}

-- SERVICES
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local Lighting = game:GetService("Lighting")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

-- PLAYER REFERENCES
local Player = Players.LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")

-- FRUIT DATABASE
local Fruits = {
    Common = {"Rocket", "Spin", "Chop", "Spring", "Bomb", "Smoke", "Falcon"},
    Uncommon = {"Ice", "Flame", "Sand", "Dark", "Diamond", "Rubber", "Light", "Magma"},
    Rare = {"Ghost", "Quake", "Love", "Spider", "Barrier", "Buddha", "Portal", "Rumble"},
    Legendary = {"Blizzard", "Shadow", "Sound", "Phoenix", "Gravity", "Pain", "Dough"},
    Mythical = {"Kitsune", "Dragon", "Leopard", "Spirit", "Mammoth", "T-Rex", "Yeti", "Venom", "Control"}
}

-- ISLAND DATABASE
local Islands = {
    ["Starter Island"] = Vector3.new(-100, 50, -100),
    ["Jungle"] = Vector3.new(1500, 50, 100),
    ["Pirate Village"] = Vector3.new(-1100, 50, 3800),
    ["Desert"] = Vector3.new(1000, 50, 4000),
    ["Frozen Village"] = Vector3.new(1100, 50, -4500),
    ["Marine Fortress"] = Vector3.new(-4500, 50, 2000),
    ["Sky Island"] = Vector3.new(500, 1500, 2000)
}

-- ============================================
-- ADVANCED GUI SYSTEM WITH IMAGE BACKGROUND
-- ============================================
local function CreateGUI()
    -- Main ScreenGui
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "DanhSigmaGUI"
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    ScreenGui.ResetOnSpawn = false
    
    if syn and syn.protect_gui then
        syn.protect_gui(ScreenGui)
    end
    
    ScreenGui.Parent = game:GetService("CoreGui")
    
    -- Main Frame with Image Background
    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Size = UDim2.new(0, 500, 0, 650)
    MainFrame.Position = UDim2.new(0.5, -250, 0.5, -325)
    MainFrame.BackgroundTransparency = 1
    MainFrame.Parent = ScreenGui
    
    -- Background Image
    local BackgroundImage = Instance.new("ImageLabel")
    BackgroundImage.Name = "Background"
    BackgroundImage.Image = "rbxassetid://" .. DanhSigma.ImageID
    BackgroundImage.Size = UDim2.new(1, 0, 1, 0)
    BackgroundImage.BackgroundTransparency = 1
    BackgroundImage.ScaleType = Enum.ScaleType.Crop
    BackgroundImage.Parent = MainFrame
    
    -- Image Overlay for better text visibility
    local Overlay = Instance.new("Frame")
    Overlay.Name = "Overlay"
    Overlay.Size = UDim2.new(1, 0, 1, 0)
    Overlay.BackgroundColor3 = Color3.new(0, 0, 0)
    Overlay.BackgroundTransparency = 0.7
    Overlay.Parent = BackgroundImage
    
    -- Title with Glow Effect
    local TitleContainer = Instance.new("Frame")
    TitleContainer.Name = "TitleContainer"
    TitleContainer.Size = UDim2.new(1, 0, 0, 70)
    TitleContainer.BackgroundTransparency = 1
    TitleContainer.Parent = MainFrame
    
    local Title = Instance.new("TextLabel")
    Title.Name = "Title"
    Title.Text = "⚡ DANH SIGMA ULTIMATE ⚡"
    Title.Size = UDim2.new(1, 0, 1, 0)
    Title.BackgroundTransparency = 1
    Title.TextColor3 = Color3.new(1, 1, 0)
    Title.TextSize = 28
    Title.Font = Enum.Font.GothamBlack
    Title.TextStrokeColor3 = Color3.new(0, 0, 0)
    Title.TextStrokeTransparency = 0.5
    Title.Parent = TitleContainer
    
    local Subtitle = Instance.new("TextLabel")
    Subtitle.Name = "Subtitle"
    Subtitle.Text = "BLOX FRUITS HACK v" .. DanhSigma.Version
    Subtitle.Size = UDim2.new(1, 0, 0, 20)
    Subtitle.Position = UDim2.new(0, 0, 1, -25)
    Subtitle.BackgroundTransparency = 1
    Subtitle.TextColor3 = Color3.new(0.8, 0.8, 0.8)
    Subtitle.TextSize = 16
    Subtitle.Font = Enum.Font.GothamBold
    Subtitle.Parent = TitleContainer
    
    -- Control Buttons
    local ControlButtons = Instance.new("Frame")
    ControlButtons.Name = "ControlButtons"
    ControlButtons.Size = UDim2.new(1, -40, 0, 40)
    ControlButtons.Position = UDim2.new(0, 20, 1, -50)
    ControlButtons.BackgroundTransparency = 1
    ControlButtons.Parent = MainFrame
    
    -- Close Button
    local CloseButton = Instance.new("TextButton")
    CloseButton.Name = "CloseButton"
    CloseButton.Text = "✕"
    CloseButton.Size = UDim2.new(0, 40, 0, 40)
    CloseButton.Position = UDim2.new(1, -45, 0, 10)
    CloseButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
    CloseButton.TextColor3 = Color3.new(1, 1, 1)
    CloseButton.TextSize = 20
    CloseButton.Font = Enum.Font.GothamBold
    CloseButton.Parent = TitleContainer
    
    -- Minimize Button
    local MinimizeButton = Instance.new("TextButton")
    MinimizeButton.Name = "MinimizeButton"
    MinimizeButton.Text = "─"
    MinimizeButton.Size = UDim2.new(0, 40, 0, 40)
    MinimizeButton.Position = UDim2.new(1, -90, 0, 10)
    MinimizeButton.BackgroundColor3 = Color3.fromRGB(255, 150, 0)
    MinimizeButton.TextColor3 = Color3.new(1, 1, 1)
    MinimizeButton.TextSize = 20
    MinimizeButton.Font = Enum.Font.GothamBold
    MinimizeButton.Parent = TitleContainer
    
    -- Drag Functionality
    local dragging = false
    local dragInput, dragStart, startPos
    
    TitleContainer.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = MainFrame.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    
    TitleContainer.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then
            dragInput = input
        end
    end)
    
    game:GetService("UserInputService").InputChanged:Connect(function(input)
        if dragging and input == dragInput then
            local delta = input.Position - dragStart
            MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X,
                                          startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    
    -- Tab System
    local TabButtons = Instance.new("Frame")
    TabButtons.Name = "TabButtons"
    TabButtons.Size = UDim2.new(1, -40, 0, 40)
    TabButtons.Position = UDim2.new(0, 20, 0, 80)
    TabButtons.BackgroundTransparency = 1
    TabButtons.Parent = MainFrame
    
    local TabContainer = Instance.new("Frame")
    TabContainer.Name = "TabContainer"
    TabContainer.Size = UDim2.new(1, -40, 1, -180)
    TabContainer.Position = UDim2.new(0, 20, 0, 130)
    TabContainer.BackgroundTransparency = 1
    TabContainer.Parent = MainFrame
    
    local Tabs = {}
    local CurrentTab = nil
    
    local function CreateTab(name, icon)
        local TabButton = Instance.new("TextButton")
        TabButton.Name = name .. "Tab"
        TabButton.Text = icon .. " " .. name
        TabButton.Size = UDim2.new(0.2, -5, 1, 0)
        TabButton.Position = UDim2.new(#Tabs * 0.2, 0, 0, 0)
        TabButton.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        TabButton.BackgroundTransparency = 0.5
        TabButton.TextColor3 = Color3.new(1, 1, 1)
        TabButton.TextSize = 14
        TabButton.Font = Enum.Font.Gotham
        TabButton.Parent = TabButtons
        
        local TabContent = Instance.new("ScrollingFrame")
        TabContent.Name = name .. "Content"
        TabContent.Size = UDim2.new(1, 0, 1, 0)
        TabContent.BackgroundTransparency = 1
        TabContent.ScrollBarThickness = 3
        TabContent.ScrollBarImageColor3 = Color3.fromRGB(255, 150, 0)
        TabContent.Visible = false
        TabContent.Parent = TabContainer
        
        local UIListLayout = Instance.new("UIListLayout")
        UIListLayout.Padding = UDim.new(0, 15)
        UIListLayout.Parent = TabContent
        
        TabButton.MouseButton1Click:Connect(function()
            if CurrentTab then
                CurrentTab.Button.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
                CurrentTab.Content.Visible = false
            end
            
            TabButton.BackgroundColor3 = Color3.fromRGB(255, 150, 0)
            TabContent.Visible = true
            CurrentTab = {Button = TabButton, Content = TabContent}
        end)
        
        table.insert(Tabs, {Button = TabButton, Content = TabContent})
        
        return TabContent
    end
    
    -- Create Tabs
    local AutoFarmTab = CreateTab("Auto Farm", "⚔️")
    local FruitsTab = CreateTab("Fruits", "🍊")
    local RaidsTab = CreateTab("Raids", "⚡")
    local TeleportTab = CreateTab("Teleport", "📍")
    local MiscTab = CreateTab("Misc", "⚙️")
    
    -- Activate first tab
    if #Tabs > 0 then
        Tabs[1].Button.BackgroundColor3 = Color3.fromRGB(255, 150, 0)
        Tabs[1].Content.Visible = true
        CurrentTab = Tabs[1]
    end
    
    -- Function to create section
    local function CreateSection(parent, title)
        local Section = Instance.new("Frame")
        Section.Name = title .. "Section"
        Section.Size = UDim2.new(1, 0, 0, 0)
        Section.AutomaticSize = Enum.AutomaticSize.Y
        Section.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        Section.BackgroundTransparency = 0.7
        Section.Parent = parent
        
        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 8)
        Corner.Parent = Section
        
        local Padding = Instance.new("UIPadding")
        Padding.PaddingLeft = UDim.new(0, 15)
        Padding.PaddingRight = UDim.new(0, 15)
        Padding.PaddingTop = UDim.new(0, 15)
        Padding.PaddingBottom = UDim.new(0, 15)
        Padding.Parent = Section
        
        local Layout = Instance.new("UIListLayout")
        Layout.Padding = UDim.new(0, 10)
        Layout.Parent = Section
        
        if title then
            local TitleLabel = Instance.new("TextLabel")
            TitleLabel.Text = "🔷 " .. title
            TitleLabel.Size = UDim2.new(1, 0, 0, 25)
            TitleLabel.BackgroundTransparency = 1
            TitleLabel.TextColor3 = Color3.new(1, 1, 1)
            TitleLabel.TextSize = 18
            TitleLabel.Font = Enum.Font.GothamBold
            TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
            TitleLabel.Parent = Section
        end
        
        return Section
    end
    
    -- Function to create toggle
    local function CreateToggle(parent, text, callback)
        local ToggleFrame = Instance.new("Frame")
        ToggleFrame.Size = UDim2.new(1, 0, 0, 40)
        ToggleFrame.BackgroundTransparency = 1
        ToggleFrame.Parent = parent
        
        local ToggleButton = Instance.new("TextButton")
        ToggleButton.Size = UDim2.new(0, 60, 0, 30)
        ToggleButton.Position = UDim2.new(1, -65, 0, 5)
        ToggleButton.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
        ToggleButton.Text = ""
        ToggleButton.Parent = ToggleFrame
        
        local ToggleCorner = Instance.new("UICorner")
        ToggleCorner.CornerRadius = UDim.new(0, 15)
        ToggleCorner.Parent = ToggleButton
        
        local ToggleCircle = Instance.new("Frame")
        ToggleCircle.Size = UDim2.new(0, 20, 0, 20)
        ToggleCircle.Position = UDim2.new(0, 5, 0, 5)
        ToggleCircle.BackgroundColor3 = Color3.new(1, 1, 1)
        ToggleCircle.Parent = ToggleButton
        
        local CircleCorner = Instance.new("UICorner")
        CircleCorner.CornerRadius = UDim.new(1, 0)
        CircleCorner.Parent = ToggleCircle
        
        local ToggleLabel = Instance.new("TextLabel")
        ToggleLabel.Text = text
        ToggleLabel.Size = UDim2.new(1, -70, 1, 0)
        ToggleLabel.BackgroundTransparency = 1
        ToggleLabel.TextColor3 = Color3.new(1, 1, 1)
        ToggleLabel.Font = Enum.Font.Gotham
        ToggleLabel.TextXAlignment = Enum.TextXAlignment.Left
        ToggleLabel.Parent = ToggleFrame
        
        local state = false
        
        ToggleButton.MouseButton1Click:Connect(function()
            state = not state
            if state then
                ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
                ToggleCircle:TweenPosition(UDim2.new(1, -25, 0, 5), "Out", "Quad", 0.2)
            else
                ToggleButton.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
                ToggleCircle:TweenPosition(UDim2.new(0, 5, 0, 5), "Out", "Quad", 0.2)
            end
            callback(state)
        end)
        
        return {
            Set = function(value)
                state = value
                if value then
                    ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
                    ToggleCircle.Position = UDim2.new(1, -25, 0, 5)
                else
                    ToggleButton.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
                    ToggleCircle.Position = UDim2.new(0, 5, 0, 5)
                end
                callback(value)
            end,
            Get = function() return state end
        }
    end
    
    -- Function to create button
    local function CreateButton(parent, text, callback)
        local Button = Instance.new("TextButton")
        Button.Text = text
        Button.Size = UDim2.new(1, 0, 0, 40)
        Button.BackgroundColor3 = Color3.fromRGB(255, 150, 0)
        Button.TextColor3 = Color3.new(1, 1, 1)
        Button.TextSize = 16
        Button.Font = Enum.Font.GothamBold
        
        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 8)
        Corner.Parent = Button
        
        Button.MouseButton1Click:Connect(callback)
        Button.Parent = parent
        
        return Button
    end
    
    -- Function to create dropdown
    local function CreateDropdown(parent, text, options, callback)
        local DropdownFrame = Instance.new("Frame")
        DropdownFrame.Size = UDim2.new(1, 0, 0, 60)
        DropdownFrame.BackgroundTransparency = 1
        DropdownFrame.Parent = parent
        
        local DropdownLabel = Instance.new("TextLabel")
        DropdownLabel.Text = text
        DropdownLabel.Size = UDim2.new(1, 0, 0, 25)
        DropdownLabel.BackgroundTransparency = 1
        DropdownLabel.TextColor3 = Color3.new(1, 1, 1)
        DropdownLabel.Font = Enum.Font.Gotham
        DropdownLabel.TextXAlignment = Enum.TextXAlignment.Left
        DropdownLabel.Parent = DropdownFrame
        
        local DropdownButton = Instance.new("TextButton")
        DropdownButton.Text = options[1] or "Select..."
        DropdownButton.Size = UDim2.new(1, 0, 0, 30)
        DropdownButton.Position = UDim2.new(0, 0, 0, 25)
        DropdownButton.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
        DropdownButton.TextColor3 = Color3.new(1, 1, 1)
        DropdownButton.Font = Enum.Font.Gotham
        DropdownButton.Parent = DropdownFrame
        
        local DropdownCorner = Instance.new("UICorner")
        DropdownCorner.CornerRadius = UDim.new(0, 6)
        DropdownCorner.Parent = DropdownButton
        
        local DropdownList = Instance.new("ScrollingFrame")
        DropdownList.Size = UDim2.new(1, 0, 0, 0)
        DropdownList.Position = UDim2.new(0, 0, 1, 5)
        DropdownList.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        DropdownList.ScrollBarThickness = 3
        DropdownList.Visible = false
        DropdownList.Parent = DropdownFrame
        
        local ListLayout = Instance.new("UIListLayout")
        ListLayout.Parent = DropdownList
        
        -- Populate dropdown
        for i, option in ipairs(options) do
            local OptionButton = Instance.new("TextButton")
            OptionButton.Text = option
            OptionButton.Size = UDim2.new(1, 0, 0, 25)
            OptionButton.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
            OptionButton.TextColor3 = Color3.new(1, 1, 1)
            OptionButton.Font = Enum.Font.Gotham
            OptionButton.Parent = DropdownList
            
            OptionButton.MouseButton1Click:Connect(function()
                DropdownButton.Text = option
                DropdownList.Visible = false
                callback(option)
            end)
        end
        
        DropdownButton.MouseButton1Click:Connect(function()
            DropdownList.Visible = not DropdownList.Visible
            DropdownList.Size = UDim2.new(1, 0, 0, math.min(#options * 30, 150))
        end)
        
        return {
            Set = function(value)
                DropdownButton.Text = value
                callback(value)
            end,
            Get = function() return DropdownButton.Text end
        }
    end
    
    -- ============================================
    -- AUTO FARM TAB
    -- ============================================
    local AutoFarmSection = CreateSection(AutoFarmTab, "Auto Farm Settings")
    
    local AutoFarmToggle = CreateToggle(AutoFarmSection, "Enable Auto Farm", function(state)
        DanhSigma.AutoFarm = state
        if state then
            warn("[DANH SIGMA] Auto Farm enabled")
        else
            warn("[DANH SIGMA] Auto Farm disabled")
        end
    end)
    
    local FarmSpeedSlider = CreateSlider(AutoFarmSection, "Farm Speed", 0.1, 1.0, 0.2, function(value)
        DanhSigma.FarmSpeed = value
    end)
    
    local FarmDistanceSlider = CreateSlider(AutoFarmSection, "Farm Distance", 5, 50, 20, function(value)
        DanhSigma.FarmDistance = value
    end)
    
    local SelectMobDropdown = CreateDropdown(AutoFarmSection, "Select Mob Type", 
        {"All", "Bandits", "Pirates", "Marines", "Bosses"}, function(option)
        warn("[DANH SIGMA] Targeting:", option)
    end)
    
    local StartFarmButton = CreateButton(AutoFarmSection, "▶ Start Auto Farm", function()
        DanhSigma.AutoFarm = true
        AutoFarmToggle.Set(true)
        warn("[DANH SIGMA] Starting Auto Farm...")
    end)
    
    local StopFarmButton = CreateButton(AutoFarmSection, "⏹ Stop Auto Farm", function()
        DanhSigma.AutoFarm = false
        AutoFarmToggle.Set(false)
        warn("[DANH SIGMA] Stopped Auto Farm")
    end)
    
    -- ============================================
    -- FRUITS TAB
    -- ============================================
    local FruitsSection = CreateSection(FruitsTab, "Fruit Collection")
    
    local AutoCollectToggle = CreateToggle(FruitsSection, "Auto Collect Fruits", function(state)
        DanhSigma.AutoCollectFruits = state
    end)
    
    local FruitPriorityDropdown = CreateDropdown(FruitsSection, "Fruit Priority", 
        {"All", "Mythical", "Legendary", "Rare", "Uncommon", "Common"}, function(option)
        DanhSigma.FruitPriority = option
    end)
    
    local AutoStoreToggle = CreateToggle(FruitsSection, "Auto Store Fruits", funct